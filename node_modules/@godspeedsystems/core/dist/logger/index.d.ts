import pino from 'pino';
import { logger } from './logger';
declare let childLogger: pino.Logger;
declare const initializeChildLogger: (options: pino.LoggerOptions) => pino.Logger<pino.LoggerOptions>;
export { logger, childLogger, initializeChildLogger };
