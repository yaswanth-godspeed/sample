import { PlainObject, GSCloudEvent, GSStatus, GSEventSource } from "@godspeedsystems/core";
import express from "express";
declare class EventSource extends GSEventSource {
    initClient(): Promise<PlainObject>;
    subscribeToEvent(eventRoute: string, eventConfig: PlainObject, processEvent: (event: GSCloudEvent, eventConfig: PlainObject) => Promise<GSStatus>): Promise<void>;
    static createGSEvent(req: express.Request, endpoint: string): GSCloudEvent;
}
declare const SourceType = "ES";
declare const Type = "express";
declare const CONFIG_FILE_NAME = "http";
declare const DEFAULT_CONFIG: {
    port: number;
};
export { EventSource, SourceType, Type, CONFIG_FILE_NAME, DEFAULT_CONFIG };
