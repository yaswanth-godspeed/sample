import { PlainObject } from '@godspeedsystems/core';
export default function generateSchema(eventsFolderPath: string, definitionsFolderPath: string, configPath: string, port: any): Promise<PlainObject>;
