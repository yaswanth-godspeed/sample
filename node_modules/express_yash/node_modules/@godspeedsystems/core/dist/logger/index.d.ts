import pino from 'pino';
import { logger } from './logger';
declare let childLogger: pino.Logger;
declare const initilizeChildLogger: (options: pino.LoggerOptions) => pino.Logger<pino.LoggerOptions>;
export { logger, childLogger, initilizeChildLogger };
